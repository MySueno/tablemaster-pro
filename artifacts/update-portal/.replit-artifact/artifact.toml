kind = "web"
previewPath = "/"
title = "TableMaster Update Portal"
version = "1.0.0"
id = "artifacts/update-portal"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19503

[services.development]
run = "pnpm --filter @workspace/update-portal run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/update-portal", "run", "build" ]
publicDir = "artifacts/update-portal/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19503"
BASE_PATH = "/"
